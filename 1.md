1. 统一使用 4个空格 作为一级缩进，不要混用空格和 Tab
2. #: 单行注释', 三引号 """ 或 ''': 多行注释
3. 数据类型：数字（整数,浮点数，复数），字符串（单引或双引号），布尔值，列表，元组（创建后不可修改），集合，字典
4. 基本运算符：+，-，*，/，**，%，//
5. Python 是动态类型语言，变量的类型由赋给它的值决定，而且可以随时改变！
6. 内置函数：print(), input(), int(), float(), str(), bool(), list(), tuple(), set(), type(), range()