# 📘 Day 20 - Python 包管理器 (PIP)

## 1. 什么是 PIP？

> **PIP** = **P**referred **I**nstaller **P**rogram（首选安装程序）

我们使用 `pip` 来安装各种Python包。**包 (Package)** 是一个可以包含一个或多个模块的Python模块。在编程中，我们不需要自己写所有工具程序，而是安装包并导入到我们的应用中使用。

---

## 2. 安装 PIP

如果还没安装pip，打开终端执行：

```bash
pip install pip
```

检查pip是否安装成功：

```bash
pip --version
# 输出示例: pip 21.1.3 from /usr/local/lib/python3.7/site-packages/pip (python 3.9.6)
```

---

## 3. 使用 pip 安装包

### 安装 NumPy

```bash
pip install numpy
```

**NumPy** 是科学计算的基础包，包含：
- 强大的N维数组对象
- 复杂的广播功能
- C/C++和Fortran代码集成工具
- 线性代数、傅里叶变换、随机数功能

```python
>>> import numpy
>>> numpy.version.version
'1.20.1'
>>> lst = [1, 2, 3, 4, 5]
>>> np_arr = numpy.array(lst)
>>> np_arr
array([1, 2, 3, 4, 5])
>>> np_arr * 2
array([ 2,  4,  6,  8, 10])
>>> np_arr + 2
array([3, 4, 5, 6, 7])
```

### 安装 Pandas

```bash
pip install pandas
```

```python
>>> import pandas
```

---

## 4. 卸载包

```bash
pip uninstall packagename
```

---

## 5. 查看已安装的包

```bash
pip list
```

---

## 6. 查看包详情

```bash
pip show pandas
```

输出：
```
Name: pandas
Version: 1.2.3
Summary: Powerful data structures for data analysis, time series, and statistics
Home-page: http://pandas.pydata.org
License: BSD
Location: /usr/local/lib/python3.7/site-packages
Requires: python-dateutil, pytz, numpy
```

加上 `--verbose` 获取更多详情：

```bash
pip show --verbose pandas
```

---

## 7. PIP Freeze - 生成依赖文件

生成当前环境中所有已安装包及版本，适合创建 `requirements.txt`：

```bash
pip freeze
```

输出：
```
docutils==0.11
Jinja2==2.7.2
MarkupSafe==0.19
Pygments==1.6
Sphinx==1.2.2
```

保存到文件：
```bash
pip freeze > requirements.txt
```

从文件安装依赖：
```bash
pip install -r requirements.txt
```

---

## 8. 从 URL 读取数据 - requests 模块

### 安装 requests

```bash
pip install requests
```

### requests 模块常用方法

| 方法 | 说明 |
|------|------|
| `get()` | 打开网络连接获取数据，返回response对象 |
| `status_code` | 检查操作状态（成功:200） |
| `headers` | 检查头信息 |
| `text` | 提取文本内容 |
| `json()` | 提取JSON数据 |

### 示例：读取文本

```python
import requests

url = 'https://www.w3.org/TR/PNG/iso_8859-1.txt'
response = requests.get(url)

print(response)              # <Response [200]>
print(response.status_code)  # 200
print(response.headers)      # 头信息字典
print(response.text)         # 网页文本内容
```

### 示例：读取 API 数据

```python
import requests

url = 'https://restcountries.eu/rest/v2/all'  # 国家API
response = requests.get(url)

print(response)              # <Response [200]>
print(response.status_code)  # 200

countries = response.json()  # 解析JSON
print(countries[:1])         # 第一个国家的数据
```

---

## 9. 创建自己的包

将大量文件组织到不同文件夹中，便于管理。

### 包结构

```
─ mypackage
    ├── __init__.py      # 必须！标识这是一个包
    ├── arithmetic.py
    └── greet.py
```

### arithmetic.py

```python
# mypackage/arithmetic.py

def add_numbers(*args):
    total = 0
    for num in args:
        total += num
    return total

def subtract(a, b):
    return a - b

def multiple(a, b):
    return a * b

def division(a, b):
    return a / b

def remainder(a, b):
    return a % b

def power(a, b):
    return a ** b
```

### greet.py

```python
# mypackage/greet.py

def greet_person(firstname, lastname):
    return f'{firstname} {lastname}, welcome to 30DaysOfPython Challenge!'
```

### 使用自定义包

```python
>>> from mypackage import arithmetics
>>> arithmetics.add_numbers(1, 2, 3, 5)
11
>>> arithmetics.subtract(5, 3)
2
>>> arithmetics.multiple(5, 3)
15
>>> arithmetics.power(5, 3)
125

>>> from mypackage import greet
>>> greet.greet_person('Asabeneh', 'Yetayeh')
'Asabeneh Yetayeh, welcome to 30DaysOfPython Challenge!'
```

> 📌 **重要：** `__init__.py` 文件是必须的！它让Python识别该文件夹为一个包。

---

## 10. 常用第三方包推荐

| 领域 | 包名 | 安装命令 |
|------|------|----------|
| **数据库** | SQLAlchemy | `pip install SQLAlchemy` |
| **Web开发** | Django | `pip install django` |
| **Web开发** | Flask | `pip install flask` |
| **HTML解析** | BeautifulSoup | `pip install beautifulsoup4` |
| **数据分析** | NumPy | `pip install numpy` |
| **数据分析** | Pandas | `pip install pandas` |
| **科学计算** | SciPy | `pip install scipy` |
| **机器学习** | Scikit-Learn | `pip install scikit-learn` |
| **深度学习** | TensorFlow | `pip install tensorflow` |
| **深度学习** | Keras | `pip install keras` |
| **网络请求** | Requests | `pip install requests` |
| **GUI** | PyQt | `pip install pyqt5` |

---

## 💻 练习题

1. **文本分析**：读取 `http://www.gutenberg.org/files/1112/1112.txt`（罗密欧与朱丽叶），找出出现频率最高的10个词。

2. **API分析**：读取猫咪API `https://api.thecatapi.com/v1/breeds`
   - 计算猫咪体重的最小值、最大值、平均值、中位数、标准差
   - 计算猫咪寿命的统计数据
   - 创建国家和品种的频率表

3. **国家API**：读取 `https://restcountries.eu/rest/v2/all`
   - 找出10个最大的国家
   - 找出10种使用最广泛的语言
   - 统计语言总数

---

> 📌 **教授寄语：** 掌握pip就像拥有了一把万能钥匙，打开Python无限可能的大门。学会利用社区的力量，站在巨人的肩膀上！

